class Solution {
public:
    int strStr(string haystack, string needle) {
     for(int i=0;i<haystack.size();i++){
        int pos=haystack.find(needle);
        if(pos!=string::npos){
            return pos;
            
        }
        else{
            return -1;
        }
     }
     return {};
    }
};
